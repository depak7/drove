A reviewer read your change and blocked it. Address every issue below.

{issues}

You are still on branch `{branch}` in the same worktree; the original plan remains in
`.vorflux/plan.md`.

Fix the issues themselves, not the symptoms. If you believe an issue is wrong, you may leave the
code as it is — but then say explicitly in your final message which issue you are rejecting and
why, so a human can settle it. Do not silently ignore anything.

Do not commit; vorflux commits your work. Do not change anything the reviewer did not raise.
